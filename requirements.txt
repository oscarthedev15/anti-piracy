# Core pipeline has NO runtime dependencies (stdlib only) — demo + tests run
# with a bare Python 3.9+ (verified on 3.9 and 3.11). The extras below are only
# for the API gateway and
# the production adapters you'll fill in.

# --- API gateway ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- optional: headless detection (JS-rendered portals) ---
# Enables aegis.live --headless (network manifest capture). After install run:
#   playwright install chromium
playwright>=1.44

# --- suggested for further build-out (uncomment as you go) ---
# imagehash>=4.3           # perceptual fingerprinting (licensed-content proof)
# opencv-python-headless   # frame sampling
# neo4j>=5.19              # graph store for attribution at scale
# pytest>=8.0              # test runner
#
# Note: DNS resolution, TLS cert fingerprints, ASN lookups and CT-log discovery
# are all implemented with the Python standard library — no extra deps needed.
